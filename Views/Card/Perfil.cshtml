@model Pet

<div class="container">
    <div class="row">
        <div class="col-md-12 d-md-flex flex-column justify-content-center align-items-center">
            <div class="card">
              @ViewBag.Error
              @Model.Nome
            </div>
        </div>
    </div>
</div>